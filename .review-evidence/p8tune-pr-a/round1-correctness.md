Reviewer agent: review-correctness
Review round: round 1 (initial Phase 4)
Reviewed head SHA: 3494c6ac1435c1616cbddc8c5058f5c715f0d14e

Summary: clean (0 critical / 0 warning findings; 2 non-blocking notes)

Findings:
- None.

Non-blocking notes:
- docs/p8tune/clean_prec_none_baseline.md:303 — Run command shows `./build/shud keliya` but the actual smoke job (.review-evidence/.../keliya_smoke_job.out:52 `./shud is compiled successfully!`) and the commented spec template at L356 both use `./shud keliya`. SHUD's Makefile emits `./shud` at the repo root, not `./build/shud`. Cosmetic typo; the artifact + job log confirm the canonical `./shud` path was used. Suggest aligning L303 with L356.
- docs/p8tune/clean_prec_none_baseline.md:301 — Build-env table cites `Slurm job ID 9620 (cn14, ExitCode 0:0, Elapsed 00:01:47)` but the included evidence file keliya_smoke_job.out contains only `wall=88 sec` + `run_exit_code=0` — no `sacct`-style JobID/ExitCode/Elapsed triplet is reproducible from the captured log alone. Values are plausible (88s + queue ≈ 107s = 1:47) but not independently verifiable from the .review-evidence bundle. Suggest appending `sacct -j 9620 -o JobID,ExitCode,Elapsed` output to the evidence file in a future round.

Per-check assessment (one line each):
1. Aggregator script logic:                  pass — `set -euo pipefail` present (L61); Plan A awk extraction of §3.1 L70-77 returns 6 rows; floor verifier (L163-198) checks heihe ncfn=7/ncfl=85/netf=0 + heihe_x4 ncfn=51/ncfl=3620/netf=0; CLI parsing handles --plan-a/--plan-b/--source/--out + unknown-arg exit 2; `bash tools/p8tune/aggregate_clean_baseline.sh --plan-a` exits 0 and emits exactly 5 tables T1-T5 (127 lines); `--plan-b` correctly gates on missing /scratch dir and exits 1 with explanatory note (not silent pass).
2. Baseline doc structure (9 sections):      pass — all 9 spec-required sections present: §codepath-equivalence (L40), §submit-template-provenance (L104), §raw-18-cell-table (L134), §cross-N-invariance-table (L189), §roi-ratio-table (L238), §solver-failure-table (L263), §keliya-smoke-anchor (L288), §decision-input-table (L384), §mode-C-tune-reference (L400). YAML metadata SHUD_pin=37be0fe ✓ / outer_pin=2582352523... ✓ (matches current HEAD ancestor; HEAD=3494c6a, parent=2582352, distance=1) / plan=A ✓ / status reflects PASS ✓. Raw-18-cell-table has 18 rows × all 15 canonical keys; cross-N-invariance-table has 30 rows (2 cases × 15 keys) with 30/30 PASS.
3. §keliya-smoke-anchor data integrity:      pass — rivqdown SHA12 `1bfe6a30856e` matches artifact L9; all 15 keys (nfe=112248, nfeLS=116421, nni=112247, nli=116421, nsetups=0, netf=5, nst=110917, npe=0, nps=0, ncfn=205, ncfl=42, lenrw=23294, leniw=53, lenrwLS=21474, leniwLS=42) match artifact L12-26 AND job log L280-285 verbatim. Build env gcc 13.3.0 + libsundials_cvode.so.6 match artifact L29-30. PR-C G3 4-way contract at L333 explicitly enumerates `{ unset, "", "0", "5" }` invocations.
4. capstone §5.1 ratio fix correctness:      pass — capstone.md L161-163 now reads `30509 | 4.526` (3 rows for N=1/4/8); diff vs prior commit shows in-place correction of nfeLS column 30517→30509 AND ratio column 4.527→4.526, matching authoritative n8_profile_verdict.md §3.4 L131 `nfeLS_median/nfe_median = 30509/6741 = 4.526` (verified arithmetically: 30509/6741 = 4.525886 → rounds to 4.526; 30509/6740 = 4.526558 → rounds to 4.527, which was the rounding-error confusion). 5-line HTML comment at L165-169 cites n8_profile_verdict.md §3.4 L131 and explains the denominator confusion (nfe vs nni). Note: change scope is wider than the brief's "4.527 → 4.526" sentence — it also fixes nfeLS typo 30517→30509 across 3 rows, which is the correct corrective edit consistent with PR-0 typo correction lineage (ADR-0003/glossary L271).
5. §codepath-equivalence proof correctness:  pass — re-ran `cd SHUD && git diff 7a1dc8f..37be0fe -- src/Equations/cvode_config.cpp 'src/Equations/*precond*' 'src/Equations/*spgmr*'` and got exact same output as embedded at clean_prec_none_baseline.md L53-75: ONLY cvode_config.cpp line 259 `SUNLinSol_SPGMR(udata, 0, ...) → SUNLinSol_SPGMR(udata, PREC_NONE, ...)` + two trailing-whitespace hunks. Verified `git ls-tree 7a1dc8f src/Equations/ | grep precond` AND `git ls-tree 37be0fe src/Equations/ | grep precond` both empty (no MD_precond_identity files at either pin — they lived transiently in 5276167 only). The doc's L88-96 explanation of "files NEVER existed at either pin" is factually correct. PREC_NONE=0 ABI claim verified against SUNDIALS 6.0.0 header.
6. Plan B template provenance fallback:      pass — §submit-template-provenance L104-132 documents SSH probe showing template missing at canonical /scratch path, then derives 5-row provenance table from tools/run_omp.sh + case_deployment_map.md heihe_x4 entry + p1e ancestor template + SHUD pin override + build flags. Plan B aggregator stub correctly fails loud (exit 1) when /scratch dir missing rather than emitting garbage.
7. Decision-input table verdict:             pass — T5/§decision-input-table L389-394 explicitly states heihe ncfl=85 + heihe_x4 ncfl=3620 both satisfy `ncfl > 0` trigger; saturation_ratio cell (heihe_x4 N=8 nli/nni = 4.527, /5 = 0.905, flagged ≥ 0.8) matches T3 output. Verdict line "hard-evidence satisfied → full 60-cell sweep" present at L396-398, citing maxl-sweep-verdict capability. Sources cite §3.1 + §3.4 (L131 / L128) correctly.
8. Removed-behavior audit:                   pass — capstone §5.1 ratio + nfeLS column fix preserves column meaning; only values updated to authoritative source. No code or behavior removal in PR. Aggregator + baseline doc are net-new additions.
9. Wrapper/proxy faithfulness:               n/a — no wrappers introduced; aggregator extracts from verdict-doc (Plan A) without re-interpreting semantics, and Plan B stub is a transparent gate, not a wrapped invocation.
10. Altitude / ownership:                    pass — tools/p8tune/ matches per-capability tool root convention (parallels tools/p8pre/, tools/cvode_stats_diff/); docs/p8tune/ matches per-capability doc root convention. Capstone §5.1 fix lives in the rightful capstone doc and is properly cross-referenced from the new baseline doc's §roi-ratio-table L242-247.

Verdict: APPROVE — All 10 correctness checks pass with hard evidence (re-ran SHUD diff matches embedded; aggregator script exits 0 emitting all 5 tables; 15-key snapshot byte-identical between artifact + job log + doc; ratio arithmetic verified). 2 cosmetic non-blocking notes (./build/shud typo + missing sacct snapshot in evidence) do not affect correctness.
