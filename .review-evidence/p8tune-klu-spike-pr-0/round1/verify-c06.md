Verifier verdict for: c06
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: PLAUSIBLE
Evidence: Code paths at `tools/p8tune.D/klu_analyze_factor.cpp:236, 272, 283` correctly emit `KLU_OOM_DETECTED case=%s ordering=%s btf=%d peak_rss_bytes=%zu reason=...` via `std::printf` (stdout) and `return 0` for all 3 OOM branches (preflight estimate, klu_factor malloc-fail, post-factor RSS exceed). Spec REQ-5 Scenario "OOM-as-data-point" (spec.md:138-143) mandates contract `exit 0 + stdout KLU_OOM_DETECTED case=<C> ordering=<O> btf=<B> peak_rss_bytes=<N>`. Tasks.md §1.15 PR-0 acceptance enumerates 6 sub-gates (a)-(f) — sub-gate (e) is "`klu_analyze_factor` PASS without OOM" (i.e., happy path on keliya, NOT a forced-OOM test). The OOM-path verification is deferred to §2.9 PR-A smoke ("16 cells either PASS or REPORTED-OOM"). Candidate's peak_rss=3MB vs CN_NODE_RAM=173GiB observation is factual — keliya cannot exercise OOM branches naturally. Risk that format/exit-code drift goes undetected until PR-A is real but spec does NOT require PR-0 to test the OOM path; the contract is correctly implemented in code and gated for PR-A. Not blocking PR-0 merge (candidate also marks `blocks_merge: no`).
Note: Code paths are author-correct; the absence of a forced-OOM unit test in PR-0 is a defensible PR-A gate concern, not a PR-0 spec violation. Reviewer's suggested `--force-oom` flag would harden PR-A but is not in current PR-0 §1.15 scope.
