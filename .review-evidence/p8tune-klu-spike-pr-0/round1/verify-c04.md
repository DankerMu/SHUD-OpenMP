Verifier verdict for: c04
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: CONFIRMED
Evidence:
- tools/p8tune.D/klu_analyze_factor.cpp:232 — preflight formula `est_pre_factor_bytes = static_cast<size_t>(j.total_nnz) * 64ULL + n * 64ULL;` counts INPUT A matrix (nnz + cols) only; carries NO L+U fill term. Symbolic->lnz/unz first become available after klu_analyze at L250 (and final numeric_lnz/unz only after klu_factor at L263), so the L228-239 preflight structurally cannot include fill.
- .review-evidence/p8tune-klu-spike-pr-0/mac_smoke_keliya_klu_factor.log:3 + L12 — concrete under-count proof: `A nnz=10255 est_bytes=770560` (matches 10255*64+1785*64=770560) vs `peak_rss_bytes=3145728` => 3145728/770560 = 4.08× actual / predicted. Same log L7 + L10 show symbolic_lnz_est=16544 (1.6× nnz) and fill_ratio=3.23 on a near-tridiagonal mesh; on heihe_x16 (~760k unknowns, irregular AMD fill) the multiplier scales worse, not better.
- klu_analyze_factor.cpp:270-287 — post-factor guard only fires when klu_factor returns (Numeric ptr or KLU_OUT_OF_MEMORY status). Linux OOM-killer SIGKILL during malloc inside klu_factor never returns control; process exits 137 with no stdout. No SIGTERM/SIGKILL handler exists in this TU to emit a fallback `KLU_OOM_DETECTED` line. This violates openspec/changes/p8tune-klu-spike/specs/klu-pattern-spike-verdict/spec.md:140-143 (REQ-5 OOM-as-data-point: exit 0 + KLU_OOM_DETECTED stdout required; aggregator must classify as rss_overflow, not Slurm task failure).
- Reachability: PR-A heihe_x16 (NumY≈760k) is in-scope per fixture L227 ("16 cells PASS … any OOM cell reported with KLU_OOM_DETECTED diagnostic"). A preflight that under-counts ~4× on a 1785-NumY tridiagonal case will under-count further on an irregular 760k case, so a heihe_x16 cell whose true factor footprint sits in the (0.7-1.0)*CN_NODE_RAM_BYTES band can pass preflight, then trip Linux OOM-killer mid klu_factor, yielding exit 137 + no diagnostic — exactly the failure REQ-5 forbids.
Note: PR-0 keliya itself does not OOM (3 MB << 173 GiB cap) but the estimator+signal-137 escape is a structural defect shipped in PR-0 binary; the reviewer's requested fix — move preflight after klu_analyze and recompute from Symbolic->lnz+unz with a ~24B/nnz * 1.5 safety factor — is the natural correction and stays inside PR-0 surface.
