Verifier verdict for: c15
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: REFUTED
Evidence: `grep -n omp_ tools/p8tune.D/*.cpp` returns only `fd_color_jacobian.cpp:55:#include <omp.h>` and `fd_color_jacobian.cpp:196: omp_set_num_threads(1);`. `klu_analyze_factor.cpp` includes (lines 33-52: algorithm, chrono, cstdint, klu.h, mach/sys/resource, cn_node_ram.h) contain NO `<omp.h>` — the binary spawns zero OpenMP regions. KLU (SuiteSparse) is a serial direct sparse LU. Wall measurement at lines 262-266 wraps only `klu_factor(...)` with `std::chrono::steady_clock`; with no OMP regions in-process, `OMP_NUM_THREADS=8` cannot perturb it. dump_adjacency.cpp likewise lacks `<omp.h>` and emits no verdict-axis timing. The referenced `spike_array.sbatch` does not exist in the diff (only `spike_run.sh` + `probe_cn_ram.sbatch`, neither sets `OMP_NUM_THREADS`). Asymmetry is intentional: fd_color needs the pin for ColPack parallel-coloring determinism (README §147); the other two have no parallel regions to pin.
Note: README §206 already documents the fd_color pin as the explicit determinism mechanism; adding `omp_set_num_threads(1)` to a binary with zero OMP regions would be cargo-culting.
