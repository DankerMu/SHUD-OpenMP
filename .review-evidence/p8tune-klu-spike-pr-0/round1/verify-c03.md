Verifier verdict for: c03
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: CONFIRMED
Evidence:
  - `ls -la tools/p8tune.D/keliya_adjacency.csc SHUD/Basins/keliya/keliya_adjacency.csc` confirms TWO CSC files exist with identical size (55564 bytes): tools/p8tune.D/keliya_adjacency.csc (mtime 05:51), SHUD/Basins/keliya/keliya_adjacency.csc (mtime 06:05). The basin-dir copy is newer (written by last `dump_adjacency` run); the tools/p8tune.D copy is the stale leftover that masked the bug.
  - tools/p8tune.D/dump_adjacency.cpp:381 `if (chdir(basin_dir.c_str()) != 0)` → process cwd becomes SHUD/Basins/<case>/. Then L437 `std::string out_path = out_prefix + "_adjacency.csc"` + L438 `write_csc(out_path, B)` → relative path resolves under basin_dir. Comment L431-436 explicitly acknowledges: "chdir means it lands in basin_dir/".
  - tools/p8tune.D/fd_color_jacobian.cpp:218 `const std::string csc_path = in_prefix + "_adjacency.csc";` happens BEFORE L241 `chdir(basin_dir.c_str())`. L222 `if (!read_csc(csc_path, csc)) return 1;` is called immediately at L221-222 — the read uses the launch-cwd path. With BASIN_ROOT default `../../SHUD/Basins` and CASE `keliya`, the read targets `tools/p8tune.D/keliya_adjacency.csc`, NOT `SHUD/Basins/keliya/keliya_adjacency.csc`.
  - tools/p8tune.D/klu_analyze_factor.cpp:164 confirms reviewer's claim about the intended convention: `std::string jbin_path = basin_root + "/" + case_name + "/" + in_prefix + "_numeric_J.bin";` — uses explicit basin_root path. Comment L160-163 says: "klu_analyze_factor runs from the spike-tool launch dir; the J binary produced by fd_color_jacobian lives in basin_dir/<prefix>_numeric_J.bin (fd_color_jacobian chdir'd into basin_dir before writing). Honor the same convention here." This proves the basin-dir convention is intentional, and fd_color_jacobian.cpp:218 simply forgot to apply it on the CSC read path.
  - tools/p8tune.D/spike_run.sh:87/91 invoke `$DUMP` (stage 1) and `$FDCJ` (stage 2) from the same parent-shell cwd (`tools/p8tune.D` per L14-15 usage doc); the chdir inside dump_adjacency does not persist to the parent shell.
Note: Reviewer's scenario is constructible from the diff — stale CSC presence in tools/p8tune.D/ is confirmed, and removing it (`rm tools/p8tune.D/keliya_adjacency.csc`) would cause stage 2 to fail at fd_color_jacobian.cpp:222 `read_csc` returning false. Fix: change L218 to `csc_path = basin_root + "/" + case_name + "/" + in_prefix + "_adjacency.csc"` (mirror klu_analyze_factor.cpp:164 pattern).
