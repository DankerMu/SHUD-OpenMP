Verifier verdict for: c09
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: CONFIRMED
Evidence: tools/p8tune.D/dump_adjacency.cpp:66-75 — comment block says "shud.o IS pulled in by Model_Data initialize() (via the g_numa_first_touch_enabled extern), so these globals are already defined. If you see a multiple-definition link error, remove the duplicates below; if you see undefined-symbol, restore them." A `grep` for `globalY`/`timeNow` in the same file returns only the comment text at L73 (no actual symbol definitions remain), so the "remove the duplicates below" guidance refers to absent code and is misleading. Chain verified: SHUD/src/ModelData/Model_Data.cpp:10 declares `extern int g_numa_first_touch_enabled`, which is defined in SHUD/src/Model/shud.cpp:50; the same TU also defines `globalY` (shud.cpp:35) and `timeNow` (shud.cpp:36). Pulling shud.o into the link via the numa extern is the sole reason globalY/timeNow resolve here — a future refactor moving g_numa_first_touch_enabled out of shud.cpp would silently break the link without touching this tool.
Note: Stale-comment + fragile transitive-symbol dependency both real; minor severity confirmed.
