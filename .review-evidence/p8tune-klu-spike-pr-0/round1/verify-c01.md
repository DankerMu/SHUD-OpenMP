Verifier verdict for: c01
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: CONFIRMED
Evidence: spec REQ-3 Scenario "keliya tool-correctness gate via independent ground-truth reference" (openspec/changes/p8tune-klu-spike/specs/klu-pattern-spike-verdict/spec.md L79-80) requires "the FD-probed numeric J for keliya SHALL be cross-checked against a brute-force dense FD on keliya (NumY ≈ 1.5K is small enough for an O(NumY) column-by-column dense baseline) — relative error ≤ 1e-6 per nonzero entry … AND PR-0 SHALL NOT merge without keliya tool-correctness PASS (both nnz exact-match and dense FD cross-check)". The implementation at tools/p8tune.D/dense_fd_cross_check.py L18 explicitly admits "We don't actually need to re-run a full dense FD"; cross_check() at L100-128 only walks the existing colored-FD binary and records max_abs/finite counts — no independent column-by-column probe is invoked. L139-144 hardcodes `max_rel_err = 0.0` with the rationalization that the distance-2 invariant makes it correct "by construction", and L147 evaluates `max_rel_err <= 1e-6` against that constant, guaranteeing PASS. The evidence TSV .review-evidence/p8tune-klu-spike-pr-0/mac_smoke_keliya_dense_fd_cross_check.tsv shows every one of 25 block pairs reports max_rel_err=0.0e+00 — including blocks with zero nonzeros, confirming the value is a literal not a measurement. No O(NumY) column-by-column dense baseline is computed anywhere in the script or evidence directory, violating the spec's explicit requirement of an "INDEPENDENT computation" (spec L77) of the same J entries.
Note: The PR-0 merge gate per spec L80 is therefore unmet — the dense FD cross-check artifact is a stub that always passes regardless of numeric J correctness.
