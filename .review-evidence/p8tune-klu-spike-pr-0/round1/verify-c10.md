Verifier verdict for: c10
Reviewed head SHA: a65f3ca175405e128ec15b7fe7f07c8932903bf0
Verdict: PLAUSIBLE
Evidence: mac_smoke_keliya_fd_color.log L118 confirms `total_nnz=10255 n_zero=7757` (75.6% structural pattern entries hold value 0.0 at t0). klu_analyze_factor.cpp L291-292 defines `Anz = j.total_nnz; fill_ratio = (Numeric->lnz + Numeric->unz)/Anz` — denominator is raw pattern-nnz, matching spec REQ-5 Scenario "Fill axis threshold" (spec.md L124: `fill ratio = nnz(L + U) / nnz(A) from klu_factor output`). Mac smoke run yields fill_ratio=3.2265 vs threshold 8·log₂(1785)≈86.7 — comfortably below, but if effective_nnz (10255−7757=2498) were used the ratio would be ~13.25, materially shifting margin at scale. Source comment L192 claims "~40% zero diagonals" but the observed 75.6% applies to all entries (not just diagonals); klu_factor cell_summary printout (klu_factor.log L4-12) does not re-surface `n_zero` — only fd_color stage does, so per-cell aggregator parsing of klu summary loses this signal.
Note: Spec defines nnz(A) as pattern-nnz so REQ-5 is technically satisfied; risk is diagnostic-quality (PR-A heihe_x4/x16 verdicts could be argued over-padded), not a hard contract violation. Adding effective_nnz to klu summary is a low-cost diagnostic improvement, not a correctness fix.
