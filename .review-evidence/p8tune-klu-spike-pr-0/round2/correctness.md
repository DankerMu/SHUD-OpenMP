```text
Reviewer agent: review-correctness
Review round: round 2 (post-fix)
Reviewed head SHA: 50d2a4bddacbfa3ef5b3e1c25d760555103c5556
Summary: Round 1 c01 (dense FD stub) and c09 (stale comment) are both ADDRESSED. F1 dense-FD probe is sound: ε formula reused via lambda, Y is not mutated, JBIN layout shared, omp_set_num_threads(1) applies to both modes, Python rel-err formula handles edge cases correctly. One NEW critical regression introduced by F4: the post-analyze RSS preflight uses Symbolic->lnz+unz which the KLU 7.12.2 header documents (and upstream source confirms at klu_analyze.c:247 + klu_analyze_given.c:370) is set to EMPTY=-1 for COLAMD and natural-via-given orderings — for those orderings the preflight performs UB (static_cast<size_t>(-2.0)) and the OOM-as-data-point gate is silently broken for 3/4 of the ordering matrix.

Round 1 verification:
- c01 (major: dense_fd_cross_check.py was not independent): ADDRESSED.
  - F1 added `--brute-force-dense` mode at fd_color_jacobian.cpp:351-377 that performs an O(NumY) column-by-column independent FD probe. Verified: ε formula via the SAME lambda (`eps_of(k)` defined at L345-348, reused by both colored and dense branches) — bytewise identical eps_of as colored mode. Y[] is never mutated (only Y_perturbed is touched and re-initialized at L361 each iteration: `for (int j = 0; j < NumY; ++j) Y_perturbed[j] = Y[j]`). Dense JBIN layout reuses the same magic+version+NumY+total_nnz+n_colors+col_ptr+row_idx+values writer at L434-446; only the output filename differs (`<prefix>_numeric_J_dense.bin` vs `<prefix>_numeric_J.bin`). omp_set_num_threads(1) at L198 is the FIRST main() statement, before the branch into colored vs dense, so determinism guarantee covers both modes. dense_fd_cross_check.py now loads BOTH binaries (L222-223), validates CSC layout congruence (L120-127 with explicit ValueError on mismatch), and computes per-entry `max |dut - ref| / max(|ref|, |dut|, 1e-12)` (L152-153). Edge-case handling for "both zero" (L146-147 skips; counts as n_zero), "one zero one nonzero" (L148-155 falls into rel_err branch; denom defaults to max(|nonzero|, 1e-12) which catches spurious-zero correctly, giving rel_err≈1.0 and failing). Threshold default 1e-6 matches spec REQ-3 L79.
  - Evidence: `.review-evidence/p8tune-klu-spike-pr-0/mac_smoke_keliya_dense_fd_cross_check.tsv` shows REAL measured max_rel_err per block — three blocks have rel_err in 2.04e-08 to 2.13e-07 range (unsat×surf, unsat×unsat, unsat×gw); rest are exact match (rel_err=0.0). All within 1e-6 threshold. PASS verdict is now data-driven, not hardcoded.
  - Both colored and dense modes report `n_zero=7757` on keliya (identical sparse fill pattern), corroborating that the dense probe is measuring the same physical quantities as the colored probe — exactly what spec REQ-3 requires.

- c09 (minor: stale-comment hazard at dump_adjacency.cpp:66-75): ADDRESSED.
  - F9 rewrote the comment block (dump_adjacency.cpp:66-68, 3 lines) to plainly state "globalY + timeNow are defined in SHUD/src/Model/shud.cpp and transitively pulled into libshud.a via MD_rhs_core.cpp's `extern int g_numa_first_touch_enabled` reference. DO NOT add duplicate definitions here — they would cause multiple-definition link errors." Removes the "remove duplicates below" misdirection that referenced absent code. The transitive dependency on g_numa_first_touch_enabled is unchanged (no shud_globals.cpp carve-out added; reviewer's option-a was deferred). Comment correctly warns against the build-breaking refactor.

New findings (round 2):

- Severity: critical
  Failure class: numeric correctness regression / undefined behavior across 3/4 ordering matrix
  Contract or invariant: spec REQ-5 Scenario "OOM-as-data-point" (openspec/changes/p8tune-klu-spike/specs/klu-pattern-spike-verdict/spec.md L140-143): "the cell SHALL exit 0 with a single `KLU_OOM_DETECTED case=... ordering=... btf=... peak_rss_bytes=... reason=...` stdout line — so the aggregator classifies it as a verdict data point, not a Slurm task failure"
  Scenario or repro: F4's NEW post-analyze preflight at klu_analyze_factor.cpp:266-279 computes `est_after_analyze_bytes = static_cast<size_t>(Symbolic->lnz + Symbolic->unz) * 24ULL * 3ULL / 2ULL`. KLU 7.12.2 header (`/opt/homebrew/Cellar/suite-sparse/7.12.2/include/suitesparse/klu.h:36`) documents these fields with the comment "only computed if the AMD ordering is chosen". Upstream KLU source verifies: `klu_analyze.c:247` sets `Symbolic->lnz = lnz; /* EMPTY if COLAMD used */` (then `Symbolic->unz = lnz` at L248), where EMPTY=-1 per `klu_internal.h`. `klu_analyze_given.c:370-371` (used by spike `--ordering natural` at L248) sets `Symbolic->lnz = EMPTY; Symbolic->unz = EMPTY;` unconditionally. Therefore for ordering ∈ {colamd, natural}:
      Symbolic->lnz + Symbolic->unz = -1.0 + -1.0 = -2.0  (as double)
      static_cast<size_t>(-2.0) is UNDEFINED BEHAVIOR per [conv.fpint] (C++ §7.10/1 — "if the truncated value cannot be represented in the destination type, the behavior is undefined"). In practice on x86_64+clang: cvttsd2si returns INT_MIN; subsequent cast to size_t yields SIZE_MAX-something or 0 depending on uop ordering. The product `static_cast<size_t>(-2.0) * 24ULL * 3ULL / 2ULL` then wraps unpredictably.
  Required test or evidence: Mac smoke evidence at `.review-evidence/p8tune-klu-spike-pr-0/mac_smoke_keliya_klu_factor.log:6` only covered keliya/amd/btf=1: `symbolic_lnz+unz=33088 est_bytes=1191168 rss_budget=129869709311` — positive number, AMD path works. The other 3 orderings (natural, colamd, and any AMD-without-BTF cell that fell back to COLAMD per KLU's adaptive logic) are NOT smoke-tested in this PR. PR-A's 16-cell sweep will hit colamd and natural cells on heihe_x16 — each is a candidate for either spurious OOM (wrap-up to SIZE_MAX-large) or spurious PASS (wrap-down to 0) depending on the cvttsd2si behavior of the production Linux toolchain. Adding a smoke run `./klu_analyze_factor --case keliya --ordering colamd --btf 0` would surface this immediately.
  Sibling surfaces: PR-A 16-cell sweep — 8 cells (any case × {colamd, natural} × {btf=0, btf=1}) all run through this UB path. heihe_x16 + colamd + btf=0 is the canonical regression target.
  Blocks merge: yes — this re-opens the same REQ-5 violation that c04 originally raised, just for a different ordering subset. The fix was correct for AMD but accidentally narrowed the working preflight from "broken for everything" to "broken for 3/4 of the matrix".
  Impact: For colamd/natural cells in PR-A: (a) silent spurious OOM emissions (aggregator classifies cell as `rss_overflow` when KLU could actually have factored), or (b) silent spurious PASS (preflight returns 0, klu_factor proceeds, then OS-killer SIGKILLs mid-malloc on heihe_x16 → exit 137 with no diagnostic → exactly the original c04 failure). Either branch corrupts PR-A's per-cell verdict matrix.
  Requested fix: Gate the new preflight on `ordering_id == 0` (AMD) only:
      ```cpp
      if (ordering_id == 0 && Symbolic->lnz > 0 && Symbolic->unz > 0) {
          const size_t est_after_analyze_bytes =
              static_cast<size_t>(Symbolic->lnz + Symbolic->unz) * 24ULL * 3ULL / 2ULL;
          ...
      }
      // else: AMD-only estimate unavailable; rely on post-factor RSS check at L303
      ```
  For colamd/natural: either accept that only the post-factor `rss_post > CN_NODE_RAM_BYTES` guard fires (which inherits c04's OS-killer concern but is the best KLU offers in those orderings), or add a getrusage-based mid-factor sampling thread (out of PR-0 scope). At minimum the gate must NOT perform UB on EMPTY values.

Non-blocking notes:
- F3 (CSC path fix at fd_color_jacobian.cpp:226): CORRECT. Mirrors klu_analyze_factor.cpp:164 path pattern `basin_root + "/" + case_name + "/" + in_prefix + "_..."`. dump_adjacency.cpp:373-430 chdir's into basin_dir before writing CSC, so the absolute path `basin_root + "/" + case_name + "/<prefix>_adjacency.csc"` resolves correctly for all 4 cases (keliya/heihe/heihe_x4/heihe_x16) — basin_root defaults to `../../SHUD/Basins` and is per-binary configurable. Mac smoke determinism evidence shows `run1 == run2` sha256 — clean tree, no stale-CSC leak.
- F4 OOM-exit path leak check: when preflight exceeds budget at L273-278, `klu_free_symbolic(&Symbolic, &common)` is called before `return 0` (L276) — no Symbolic leak. Other OOM-exit paths at L295 (klu_factor OOM) and L306 (post-factor RSS overflow) also call free_symbolic/free_numeric. PASS for AMD path; the regression above is the broader concern.
- F5 chi gate (fd_color_jacobian.cpp:250-255): correct logic. case_name == "keliya" → threshold=30; else (heihe / heihe_x4 / heihe_x16 / any other) → 50. FAIL-OPEN for unknown case names is acceptable here because F10's shell whitelist at spike_run.sh:54-57 enumerates the 4-case set BEFORE invoking fd_color_jacobian — direct-CLI invocation of fd_color_jacobian with `--case unknown_basin` is out of spike scope and the threshold=50 fallback is a reasonable upper bound for any plausible basin (heihe at 6335 elements still has χ ≤ 50; out-of-distribution basins would either pass at χ ≤ 50 or be caught at CSC read by other gates).
- F10 case whitelist (spike_run.sh:54-57): only allows {keliya, heihe, heihe_x4, heihe_x16}. Strict — does NOT permit `heihe_x4_test`, `keliya_dev`, etc. Correct per spec REQ-4 L89. Adding a new basin requires both the shell whitelist edit and an OpenSpec change amendment, which is the right discipline.
- F1 dense FD probe — minor inefficiency: the `Y_perturbed[j] = Y[j]` reset loop at L361 is O(NumY) per iteration, total O(NumY²) just for the reset (1785² ≈ 3.2M operations for keliya, ~10ms). Could be optimized to "restore only Y_perturbed[k_prev]" between iterations (O(1) per iter) but at NumY=1785 the overhead is trivial vs the ~30s spent in rhs_core. Acceptable for PR-0; trivial future micro-optimization.
- F1 dense FD probe — REAL but subtle scope note: the "brute-force dense FD" only fills entries on the same CSC pattern (L367-372 iterates `csc.col_ptr[k]..csc.col_ptr[k+1]`). If the adjacency pattern itself omits a true non-zero entry, dense FD would NOT catch it because it never probes rows outside the CSC's per-column row_idx slice. This is acceptable because pattern correctness is separately gated by `verify_adjacency_keliya.py` (REQ-3 nnz exact-match). The dense FD's job is to validate that the COLORED-FD methodology produces the same numbers as a per-column-independent FD on the SAME pattern — that's what spec L79 says ("relative error ≤ 1e-6 per nonzero entry") and that's what's now measured.
- F11 README determinism repro recipe (README.md:209-218): self-contained `cd basins/keliya && fd_color_jacobian && sha256sum > /tmp/first.sha && fd_color_jacobian && diff first.sha <(sha256sum)`. Reproduces determinism check. PASS.
- c10 deferred per verifier: 75.6% n_zero FD entries (n_zero=7757/10255) still applies in round 2 (fd_color_jacobian L457 still emits `n_zero=N` summary). Per the round 2 task brief, NOT re-raised. PR-B aggregator may surface `effective_nnz = total_nnz - n_zero` as a second data point.
```
