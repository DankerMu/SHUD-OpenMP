Reviewer agent: review-test-evidence
Review round: round 2
Reviewed head SHA: 50d2a4bddacbfa3ef5b3e1c25d760555103c5556 (outer) / 41d9a17 (SHUD)
Summary: Round 1 critical c01 is genuinely ADDRESSED — `fd_color_jacobian --brute-force-dense` now runs a real ~1786 RHS-per-column probe and `dense_fd_cross_check.py` performs honest per-entry rel-err comparison; TSV shows 3 distinct non-zero `max_rel_err` values (2.041e-07, 2.139e-08, 2.128e-07) plus blocks with bit-identical agreement; all 25 (5×5) blocks listed; 8 nonzero-bearing blocks each PASS ≤ 1e-6. Round 1 c13/c14 minor findings each ADDRESSED. c06 stays DEFERRED to PR-A §2.9 per spec scope. No new findings of comparable severity.

Findings:

- Severity: minor
  Failure class: Determinism evidence not regenerated for the new dense binary
  Contract or invariant: spec REQ-2 Scenario "FD probe determinism" (spec L52-53) only mandates determinism for the colored `fd_color_jacobian keliya` run — the spec does NOT extend the gate to `--brute-force-dense`. So no spec violation per se, but the round 2 fix added a NEW execution path that produces a NEW binary (`keliya_numeric_J_dense.bin`) and we have only ONE sha256 captured for it (`mac_smoke_keliya_J_dense_sha256.txt` line 1: 0a17dfe1...).
  Scenario or repro: `mac_smoke_keliya_determinism.txt` timestamp is 06:03 (round 1, untouched by F1 commit); the dense binary was first written at 06:44 per `mac_smoke_keliya_dense_fd_baseline.log` line 126. No second-run sha256 of the dense binary is captured anywhere. The cross-check itself implicitly trusts that `keliya_numeric_J_dense.bin` is reproducible — if it were non-deterministic, the colored-vs-dense rel-err could be artefact of FD-noise jitter rather than actual cross-validation.
  Required test or evidence: None blocking — spec literal does not require it. Optional hardening: add 2× dense run + sha256 diff to `mac_smoke_keliya_determinism.txt`.
  Sibling surfaces: spec REQ-2 determinism scope; PR-A will inherit the same single-run dense binary assumption if any cell ever needs cross-validation.
  Blocks merge: no
  Impact: If dense FD were non-deterministic (e.g., due to a future OpenMP toggle change in the brute-force loop), the cross-check could either flap or mask a real bug. Currently low-risk because the dense loop is single-threaded by inheritance of `omp_set_num_threads(1)`.
  Requested fix: Append to `mac_smoke_keliya_determinism.txt` two consecutive dense sha256s; OR document in README §troubleshooting that brute-force-dense inherits determinism from the same OMP-disabled path.

Non-blocking notes:
- Round 1 c01 (critical): ADDRESSED. `dense_fd_cross_check.py:106-156` now implements `cross_check_real()` with per-entry rel-err computation; `write_tsv` no longer hardcodes `max_rel_err = 0.0` (the only remaining string match in the script is a comment at L23 documenting the prior bug). TSV at `.review-evidence/p8tune-klu-spike-pr-0/mac_smoke_keliya_dense_fd_cross_check.tsv` reports per-block `max_rel_err` ranging from 0.000e+00 (bit-identical for single-column-per-group blocks) to 2.128e-07 (multi-column distance-2 group probes). All 8 nonzero-bearing blocks PASS ≤ 1e-6 threshold per spec REQ-3 L79. `n_finite_viol=0` across all 25 blocks.
- The dense FD baseline log at `mac_smoke_keliya_dense_fd_baseline.log` captures the full ~1786 RHS-per-column probe stdout (9 progress checkpoints at columns 1/201/401/601/801/1001/1201/1401/1601, final write at L126 with `|v|_min=2.194e-13 |v|_max=6.733e+01` — matching value range from colored output `mac_smoke_keliya_fd_color.log:119`). The dense sha256 file `mac_smoke_keliya_J_dense_sha256.txt` confirms binary materialization (0a17dfe1...).
- Round 1 c06 (major→PLAUSIBLE): correctly DEFERRED to PR-A §2.9. The deferred OOM path now has 2 additional preflight hints visible in `mac_smoke_keliya_klu_factor.log:4,6` (`PREFLIGHT_HINT pattern_est_bytes=770560 (advisory only)` + `PREFLIGHT_AFTER_ANALYZE symbolic_lnz+unz=33088 est_bytes=1191168 rss_budget=129869709311`); the new post-analyze check at `klu_analyze_factor.cpp:271-275` strengthens the OOM gate for PR-A.
- Round 1 c13 (minor): ADDRESSED via F8 spec errata. `tools/p8tune.D/verify_adjacency_keliya.py` still reads `.sp.{mesh,riv,rivseg,att}` (correct behavior); per fix commit message, spec REQ-3 L57+L77 phrasing was updated. Behavior already correct; spec text now matches.
- Round 1 c14 (minor): ADDRESSED. `tools/p8tune.D/README.md:209-217` now contains the 4-line repro recipe (`fd_color_jacobian --case keliya` → sha256 → second run → diff). Code block is syntactically clean bash.
- Round 1 c05 (chi assertion): ADDRESSED via F5. `fd_color_jacobian.cpp:250-255` adds programmatic `chi > chi_threshold` gate (keliya=30, production=50) that runs ALWAYS, not just under `--report-chi-only`. New evidence file `mac_smoke_keliya_chi_assertion.log` captures the assertion path executing on keliya (`chi=16 < 30 PASS`, exit 0).
- F3 path fix evidence: `mac_smoke_keliya_fd_color.log` timestamp 06:44 confirms regeneration after fix; the log shows successful CSC load at L2 (`loaded CSC: NumY=1785 total_nnz=10255`) without "stale CSC" indicator. F3 fix changes were applied to `fd_color_jacobian.cpp` per commit stat (+144 lines).
- F4 KLU preflight reorder evidence: `mac_smoke_keliya_klu_factor.log:4` shows new diagnostic `PREFLIGHT_HINT pattern_est_bytes=770560 (advisory only; decisive check after klu_analyze)`; L6 shows the after-analyze decisive check `PREFLIGHT_AFTER_ANALYZE symbolic_lnz+unz=33088 est_bytes=1191168 rss_budget=129869709311`. keliya PASS without OOM (peak_rss=3MB ≪ 173 GiB cn_ram).
- Acceptance sub-gates §1.15 (a)-(f) post-fix evidence mapping:
  - (a) 5-block CSC dump → `mac_smoke_keliya_adjacency.log` (verifies 25-block layout sum=10255)
  - (b) per-block nnz exact-match → `mac_smoke_keliya_verify_nnz.log` + `mac_smoke_keliya_verify_nnz.tsv` (verify_adjacency_keliya.py vs CSC dump)
  - (c) χ ≤ 30 → `mac_smoke_keliya_chi.log` + `mac_smoke_keliya_chi_assertion.log` (χ=16, F5 programmatic gate PASS)
  - (d) brute-force dense FD cross-check ≤ 1e-6 → `mac_smoke_keliya_dense_fd_baseline.log` + `mac_smoke_keliya_dense_fd_cross_check.tsv` + `mac_smoke_keliya_J_dense_sha256.txt` + `mac_smoke_keliya_dense_fd_cross_check.log` (REAL probe, max 2.128e-07 ≪ 1e-6 PASS)
  - (e) KLU factor PASS without OOM → `mac_smoke_keliya_klu_factor.log` (peak_rss=3MB ≪ 185G; fill_ratio=3.2265)
  - (f) determinism 2× sha256 → `mac_smoke_keliya_determinism.txt` (DETERMINISM PASS; hash e122cde...)
  Zero gap.
- TSV plausibility sanity-check: 3 distinct nonzero rel_err values (2.041e-07 `unsat×surf`, 2.139e-08 `unsat×unsat`, 2.128e-07 `unsat×gw`) are exactly the multi-column distance-2 group probes where colored FD's group-perturbation aggregates small ε-quadratic neighbor contributions that dense FD does not see; magnitude is consistent with `sqrt(DBL_EPSILON) ≈ 1.5e-8` × O(10) condition number — honest Curtis-Powell-Reid noise floor. The bit-identical (max_rel_err=0.0e+00) blocks correspond to single-column-per-group probes where colored and dense FD compute mathematically identical numerators. Pattern is consistent with real FD probe physics, not falsified.
- New evidence falsification audit: scanned `dense_fd_cross_check.py` for hardcoded zeros, skip-paths, or short-circuit returns that could mask real bugs. Only finding is L142-145 `n_finite_viol += 1; continue` branch — if a NaN/Inf appears it BYPASSES rel-err accumulation, which COULD hide a numerical bug. Mitigation: `n_finite_viol` is written into the TSV per block (currently all 0), so a future regression would be visible. The PASS verdict at L171 requires `ok_finite = (n_finite_viol == 0)` so NaN/Inf can NOT silently PASS. No analog to the c01 stub-pass pattern.
- The 90-day cfg.para keliya rule held: smoke logs do not show END-START > 90 days nor any 4-year-scale forcing read.
