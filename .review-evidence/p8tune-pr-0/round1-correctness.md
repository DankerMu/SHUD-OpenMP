Reviewer agent: review-correctness
Review round: round 1 (initial Phase 4)
Reviewed head SHA: e3aa6dbc977b2fad3fef49a6a099c74fe029b872

Summary: clean with 1 non-blocking note (downstream-derived ratio 4.527 vs 4.526 in capstone §5.1 table; outside PR-0 spec scope, fix recommended in PR-A if Plan A baseline anchors include this table).

Findings:
- None.

Non-blocking notes:
- `docs/p8pre/capstone.md:161-163` — the §5.1 "Step 1 PR-A baseline data" table now reads `heihe_x4 ... nfeLS=30509 ... nfeLS/nfe=4.527` after PR-0 fixed the nfeLS typo. The strictly-correct ratio is `30509 / 6741 = 4.5260...`, which renders as `4.526` per `docs/p8pre/n8_profile_verdict.md:129-131` §3.4 (authoritative). The capstone narrative at L165 + L167 already reports `r_max = 4.526` (canonical) — the residual `4.527` in the table column is the only remaining mathematical inconsistency. Spec `p8pre-doc-state-correction/spec.md:51` only constrains the integer `nfeLS` field, so PR-0 acceptance is unaffected. Recommend rolling the column update (`4.527` → `4.526` × 3 rows) into PR-A `clean-prec-none-baseline` since that capability re-anchors §3.1 numbers anyway (design D2 L73-81 enumerates the §3.1 anchor scope).

Per-check assessment (one line each):
1. Citation accuracy:                  pass — heihe nfeLS=12632/ncfn=7 + heihe_x4 nfeLS=30509/ncfn=51 verified at `docs/p8pre/n8_profile_verdict.md:72-77`; outer `e442ce8` verified via `git show e442ce8 --stat` as the SHUD pointer-bump commit (msg "bump pointer 5276167 → 37be0fe"); SHUD `37be0fe` verified as `openmp-baseline` HEAD via `git -C SHUD log 37be0fe`; date 2026-06-27 matches today.
2. Cross-doc consistency:              pass — `grep -rnE 'ncfn ?< ?6\b|ncfn ?< ?47\b' docs/` returns zero matches; all 3 future-gate sites (ADR-0003 L70, identity_spike_verdict.md L169, capstone.md L236) now use the corrected `ncfn_candidate ≤ 7 ∧ ≤ 51` wording with the explicit "6/47 retained ONLY as negative-control anchor" disclaimer per spec scenarios.
3. Typo consistency:                   pass — `grep -rnE '30518|30517' docs/ openspec/glossary.md SHUD_openMP_master_plan.md` returns one match at `SHUD_openMP_master_plan.md:2400` which is descriptive narrative inside §P8-tune.C bullet ("future-gate 6/47 → 7/51 / `nfeLS` typo 30518/30517 → 30509"), explicitly describing what was fixed — acceptable per spec intent.
4. Numerical consistency (4.527/4.526): note — see Non-blocking notes (capstone §5.1 table column out of spec scope).
5. Glossary term semantics:            pass — `mode-C-tune` (glossary L294-296) correctly mirrors design D10 L167 cross-maxl A4 semantics + design D9 mode-C-tune anchor structure; explicit hydrology-G7 fallback caveat present. `SHUD_SPGMR_MAXL` (glossary L298-300) correctly lists value domain `{unset, "", "0", "5", "10", "15", "20", "30"}` per spec `spgmr-maxl-env-hook/spec.md` L9-34, includes the 4 mandatory safety constraints (NEVER PREC_LEFT, NEVER preconditioner, default-unset bit-identical, abort via `myexit(ERRCVODE)` on invalid). One latent design/spec mismatch (design D10 L167 says "cross-N N=1/4/8" while sweep spec L43 narrows to `N ∈ {1,8}`) is inherited from design — not introduced by PR-0, and is design D9's documented "baseline N=4 is OMP-neutrality regression detector only" intent.
6. Master plan §P8-tune.C consistency: pass — master plan L2396-2443 4-capability roster + 6-PR table + 8-gate verdict + 6 ADR-0004 branches all match design D11 L185-208 PR sequence, design D8 L137-156 8-gate table, and spec `p8pre-doc-state-correction/spec.md:87-89` Scenario "Master plan §P8-tune.C section added". PR-F is correctly marked conditional (GO branch only) per design D11 L195.
7. Forward-link correctness:           pass — all referenced capabilities (`clean-prec-none-baseline`, `spgmr-maxl-env-hook`, `maxl-sweep-verdict`) exist as real spec.md files under `openspec/changes/p8tune-spgmr-maxl/specs/`; `p8pre-doc-state-correction` exists at the same path; ADR-0004 is consistently labelled `(TBD)` or `(forthcoming)` everywhere it appears (master plan L2403, ADR-0003 L70+L199, capstone §6.4.3 L239 + §9.1 L311 + §9.2 L317, p8pre_summary.md §Forward note L74, glossary L296+L300) — no broken link asserts ADR-0004 already exists.
8. Cleanup SHA accuracy:               pass — outer `e442ce8` is the SHUD pointer-bump commit (`Date: Sat Jun 27 05:33:36 2026`, msg "chore(SHUD): bump pointer 5276167 → 37be0fe (p8pre design D8 cleanup)"); SHUD `37be0fe` is in the `openmp-baseline` log immediately preceding `5276167` (revert commit "revert(p8pre): design D8 PREC_NONE fall-back per ADR-0003 NO-GO"); the cleanup enumeration in ADR-0003 L89-97 + capstone §9.1 L300-309 lists all 6 mandated D8 actions with consistent `[x]` checkboxes for completed and `pending (non-blocking)` for deferred items (Timer bucket + baseline/p8pre branch close + spec L26 wording) — matching the spec L60-72 audit-trail requirement.
9. Removed-behavior audit:             pass — each `ncfn < 6 / ncfn < 47` replacement preserves the original "future-gate" semantics while only correcting the numbers (6/47 negative-control floor → 7/51 cleaned-PREC_NONE floor) and explicitly disclaiming the original 6/47 as negative-control anchor. ADR-0003 §Consequences L70 + identity_spike_verdict §6.2 L169 + capstone §6.4.3 L236 all carry the corrected wording verbatim per spec scenarios; capstone §9.2 L317 also updated (was missed in spec L24-26 but covered by spec L22-26 "Prerequisite 1" bullet at L313).
10. Altitude / ownership:              pass — master plan §P8-tune.C placement after §P8-precond.0 (master plan L2356) follows existing precedent (master plan owns epic-level anchors for §P1, §P1c, §P1d, §P1e, §P8-precond.0 chapters; epic working docs live under `docs/p1e/`, `docs/p8pre/`, future `docs/p8tune/`). Doc body intentionally references `openspec/changes/p8tune-spgmr-maxl/` and a future `docs/p8tune/` directory rather than embedding implementation detail in master plan itself.
