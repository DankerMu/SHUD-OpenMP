Verifier verdict for: F1 — spike binaries MD leak on success path
Reviewed head SHA: 09a815dbcab9eabbddcad9550c706ddfa8636519
Verdict: CONFIRMED
Evidence:
- tools/p8tune.D/dump_adjacency.cpp:398-402,440-442 — allocates `FileIn *fin = new FileIn;`, `FileOut *fout = new FileOut;`, `Model_Data *MD = new Model_Data(fin, fout);` then on success path runs only `std::fflush(stdout); std::fflush(stderr); return ok ? 0 : 1;` — no `delete MD; delete fout; delete fin;` between allocation and return.
- tools/p8tune.D/fd_color_jacobian.cpp:282-286,471-473 — allocates `FileIn *fin = new FileIn;`, `FileOut *fout = new FileOut;`, `Model_Data *MD = new Model_Data(fin, fout);` then on success path runs only `std::fflush(stdout); std::fflush(stderr); return 0;` — no delete calls.
- Error-path delete pattern present at fd_color_jacobian.cpp:308 (`delete MD; delete fout; delete fin; return 1;` on NumY mismatch), :318 (globalY null guard), :432 (fopen failure) — confirming the expected ownership convention is "delete before non-zero return", which the success path violates.
- `git show a7eb922 -- tools/p8tune.D/` confirms commit a7eb922 (2026-06-28 "fix(p8tune.D): _exit(0) instead of dtor for spike binaries") deleted the three `delete MD; delete fout; delete fin;` lines from BOTH files' success path (replaced with `_exit(ok ? 0 : 1)` / `_exit(0)`). PR-400 (head 09a815d) reverted `_exit → return` (visible in current file: lines 440-442 and 471-473 now flush + return) but did NOT restore the three delete statements — they remain absent on the success path.
- Therefore `return 0` from `main()` in either binary destroys the local stack frame WITHOUT invoking `~Model_Data()`, `~FileIn()`, `~FileOut()` — the heap objects pointed to by MD/fin/fout are leaked. The PR-body / commit-message / design.md §D5 claim that "destructor chain runs to completion" is contradicted by the source: dtor of the heap-allocated `Model_Data` is unreachable on the success path in both binaries.
Note: Linus-preferred fix is to drop `new` and stack-allocate (`Model_Data MD(fin, fout);` with similar promotion of fin/fout), eliminating the new/delete imbalance entirely; minimum fix is restoring the three deletes before each success `return`.
