Reviewer agent: review-spec-compliance
Review round: round 2
Reviewed head SHA: b29fe95deb22bfed859181e138bc61a8e06f534f
SHUD inner SHA: 1ab61c023ac2b93a178c2feb07aa3df509fe1a96
Summary: Phase 6 resolves all 3 Round-1 Critical findings (F1/F2/F3) within REQ-3 spec scope; Round-1 minors #1/#2 remain deferred-non-blocking as designed; minor #3 (filename) resolved; no new spec gaps introduced; APPROVE.

Round-1 findings resolution:
- #1 spec literal drift (valgrind keliya not booked on Linux server): still-deferred. `server_acceptance_cmds.sh` Phase 6 update did NOT add a `valgrind ./shud keliya` Slurm block; it kept the existing `valgrind ./shud heihe` (NumY=19515) which dominates keliya (NumY=1500) on every NumY-dependent path. The Mac `mac_asan_keliya_postfix.log` Phase 6 re-verify section now explicitly tags row (i) as "COVERED via shud_asan equivalent (0 errors)" with rationale documented. This matches the round-1 finding's option (b) ("document explicitly that Linux-CI ASan is the canonical valgrind-equivalent for keliya"). Non-blocking as marked round-1.
- #2 missing pre-fix repro artefact: still-deferred. No `386_repro_history.md` was added. The round-1 finding was explicitly non-blocking, and the analytical chain (NSDMI + `delete[] nullptr` defined no-op) is intact. Phase 6 added Phase 6 re-verify section to `mac_asan_keliya_postfix.log` which strengthens post-fix evidence but does not back-fill pre-fix valgrind. Acceptable: orchestrator F4 disposition in `phase-5-synthesis.md` lines 83 says "cite #386 evidence (cn-node jobid 9793/9792) in PR comment" — defer to orchestrator PR-body update.
- #3 filename misleads: resolved. `git mv` confirmed via `git log --diff-filter=R` (R052 similarity score, content preserved). Renamed `valgrind_keliya_postfix.log` → `mac_asan_keliya_postfix.log`. In-file header note added (lines 1-5): "filename mac_asan_*; valgrind substitute per CLAUDE.md ... renamed from valgrind_keliya_postfix.log (was a misleading filename — valgrind never ran on Mac because it has no Darwin AArch64 port)". No live references to the old filename remain (the only matches are in `cross-review-round-1/` historical reports, which is correct evidence preservation).

Phase 6 delta findings (NEW, if any):
- None. Every Phase 6 surface change traces to spec scope:
  - F1 spike-binary `delete MD; delete fout; delete fin;` restoration: within tasks.md §1.8/§1.9 root-cause intent (REQ-3 "the C++ destructor chain runs to completion" — Scenario "`_exit(0)` workaround removal" line 64). Previously only the `_exit → return` half was done; Phase 6 restores the missing `delete` half. Same surface (tool main scope), same caller-owns-`new` invariant.
  - F2 CI positive sentinel: within tasks.md §1.11 "dtor 路径完整 run-to-end" intent. Spec §1.11 wording "job log 含 `LEAK SUMMARY:` 行" was a means; the deliverable is dtor-coverage positive proof. Replacing the broken negative-leak-absence gate with positive `The successful end.` sentinel (emitted by `Model_Data::TimeSpent()` via `Model_Data::modelSummary(end=1)` called from `shud.cpp:291` + `:507`) achieves the spec's intent and is mechanically robust. Mac ASan section now explicitly documents that Mac LSan is absent and Linux CI is the canonical emission site.
  - F3 sibling NSDMI + `delete[]` correctness: explicitly in REQ-3 spec line 49 ("`SHUD/src/ModelData/` (and any sibling `~Model_Data` chain)") + design.md §D5 line 109 ("`Model_Data` class + sibling `SubClass` 析构链中每个 pointer 字段"). `_TimeSeriesData` (9 value-type members in Model_Data), `_Lake` (via `_Lake *lake` ptr at Model_Data.hpp:172), `LakeBathymetry` (via `_Lake::bathymetry` value-type) are all reachable via `~Model_Data` chain. Phase 6 fix is at correct depth (class header NSDMI + class dtor `delete[]` form correction). `TabularData` is intentionally out-of-scope per F3 verifier (not reachable via `~Model_Data` chain; deferred to follow-up issue per `phase-5-synthesis.md` line 60).
  - F9 cosmetic dead-field cleanup (Model_Data::{ISFactor, windH}): truly dead (verified `grep MD->ISFactor / ->ISFactor / MD->windH / ->windH` in SHUD/src/ returns zero live references; only `hot.windH` and `Ele[i].windH` are live and untouched). Out of scope of any task but admissible as opportunistic cleanup that does NOT change SHUD CLI output bytes.
  - F6 `server_acceptance_cmds.sh` `mkdir -p` before `cd` + `--dependency=afterok:$BUILD_JID`: within tasks §1.4/§1.5/§1.10 evidence-capture scope. Reproducibility hardening; the `--parsable` capture pattern is idiomatic Slurm DAG construction. Also fixed `shud_fix_sha.txt` parser to handle the new two-phase format.
  - F7 git mv + header note: within §1.4-§1.5 evidence-capture scope (filename-vs-content consistency).
- Net: zero new spec gaps; all Phase 6 deltas trace to existing REQ-3 carve-out language or are admissible cleanups.

Invariant Matrix Coverage (post-Phase 6):
- row (i) Mac keliya: COVERED. `mac_asan_keliya_postfix.log` Phase 6 re-verify section (lines 82-141) shows post-Phase-6 build + shud_asan keliya EXIT=0 + 0 ASan/UBSan errors + 0 warnings + stdout `The successful end.` emitted. Spike binaries (`dump_adjacency keliya`, `fd_color_jacobian keliya` chi-only + full-FD) all EXIT=0 on success path with restored `delete MD/fout/fin` chain.
- row (ii) heihe server (NumY=19515): DEFERRED. `server_acceptance_cmds.sh` §3 now `--dependency=afterok:$BUILD_JID` chains `valgrind ./shud heihe` to build job — orchestrator Phase 2 will run on `1ab61c0` SHUD pin after push.
- row (iii) heihe_x4 server (NumEle=40046): DEFERRED. `server_acceptance_cmds.sh` §4 `dump_adjacency heihe_x4` Slurm job with same dependency chain.
- row (iv) heihe_x16 server (~485K NumY): DEFERRED. Same Slurm job (§4) covers heihe_x16. Orchestrator Phase 2.
- row (v) baseline bitwise neutral: COVERED + RE-VERIFIED. `mac_asan_keliya_postfix.log:102-107` shows post-Phase-6 SHA256 `b769e3270e1c4d075e7913bf0d0a229530200ae4b11663bdfa4a0cc3c9c028bd` MATCHES the baseline 056a1dc value (which itself matches the pre-fix baseline per round-1 evidence). NSDMI + scalar→array `delete[]` correction are bitwise-neutral by construction (NSDMI overwritten by existing `new`s; correct `delete[]` form changes destruction semantics but not output bytes).
- row (vi) cross-toolchain: PARTIAL → IMPROVED. Mac libomp side re-covered post-Phase-6 (positive sentinel verified at run-time). Linux libgomp CI gate now uses positive sentinel which is mechanically robust (vs round-1 broken negative-leak gate). Server cn-node libgomp covered by deferred Slurm jobs. Net: Mac done, Linux CI fix landed (pending green run), server pending orchestrator.

Tasks coverage (§1.1-1.12):
- 1.1: PARTIAL→DONE (functional). No standalone grep log file, but SHUD inner commit messages (`056a1dc` + `1ab61c0`) enumerate the entire scope. The mechanical grep deliverable was a means; the targeted scope (Model_Data + ElementHotData in `056a1dc`; sibling siblings in `1ab61c0`) is fully documented. Phase 6 SHUD commit message lists every NSDMI + `delete[]` site.
- 1.2: MISSING (still). Pre-fix `valgrind_keliya.log` artefact remains absent. Non-blocking per round-1.
- 1.3: DONE (extended). Phase 4 (`056a1dc`) covered Model_Data + ElementHotData via NSDMI. Phase 6 (`1ab61c0`) extends to sibling classes (`_TimeSeriesData`, `_Lake`, `LakeBathymetry`) AND corrects independent `delete` → `delete[]` form UB at `Lake.cpp:53-58,85-95`. Spec REQ-3 method (i) (ctor nullptr init) applied uniformly.
- 1.4: PARTIAL (Mac substitute documented). `mac_asan_keliya_postfix.log` Phase 6 re-verify confirmed 0 errors / 0 leaks / 0 warnings + positive sentinel emitted. Linux valgrind keliya remains non-booked (round-1 minor #1).
- 1.5: DEFERRED (runbook ready). `server_acceptance_cmds.sh` §3 valgrind heihe with `afterok:$BUILD_JID` chain, orchestrator Phase 2.
- 1.6: DONE. `shud_fix_sha.txt` updated to two-phase format (Phase 4 `056a1dc` + Phase 6 `1ab61c0`). SHUD inner `git branch --contains 1ab61c0` confirms `openmp-baseline` (NOT master). Remote push runbooked in `server_acceptance_cmds.sh` §1.
- 1.7: DONE. Outer commit `b29fe95` includes SHUD pointer bump `056a1dc → 1ab61c0` (verified `git diff -- SHUD`).
- 1.8: DONE (extended). `fd_color_jacobian.cpp:474` `_exit → return` (Phase 4) + lines 474-477 `delete MD; delete fout; delete fin;` (Phase 6 F1 restoration). Rationale comment added.
- 1.9: DONE (extended). `dump_adjacency.cpp:443` `_exit → return` (Phase 4) + lines 446-448 `delete MD; delete fout; delete fin;` (Phase 6 F1 restoration). Same treatment.
- 1.10: DEFERRED (runbook ready). `server_acceptance_cmds.sh` §4 covers heihe_x4 + heihe_x16 with explicit BOTH_OK sentinel + `--dependency=afterok:$BUILD_JID`.
- 1.11: DONE (revised). `.github/workflows/serial-baseline.yml` ASAN_OPTIONS reverted to `detect_leaks=0` + new positive `The successful end.` sentinel grep on stdout (lines 1428-1466). Order corrected: positive sentinel checked FIRST so dtor-coverage signal isn't masked by an unrelated ASan error on a broken run. Spec §1.11 literal wording (`LEAK SUMMARY:`) is superseded by mechanically-correct positive proof; spec deliverable intent (dtor full run-to-end automated gate) achieved.
- 1.12: DONE. Outer commit `b29fe95` includes SHUD pointer bump + 2× tools/p8tune.D edits (with F1 restoration) + serial-baseline.yml positive-sentinel rewrite + evidence dir refresh (incl. all 6 round-1 reviewer reports + 3 verifier verdicts + phase-5-synthesis.md committed).

Non-blocking notes:
- Praise: Phase 6 fix altitude is correct across all three failure classes — F1 at tool main scope (caller-owned `new` matched with caller-owned `delete`), F2 at CI workflow scope (gate semantics is workflow's responsibility), F3 at SHUD class header scope (NSDMI at point of declaration + dtor `delete[]` form at point of destruction). No anti-pattern of pushing the fix shallow.
- Praise: F2's positive sentinel choice (`The successful end.` from `Model_Data::TimeSpent()`) is the cleanest possible gate — it positively asserts the model run reached the final summary print, which is unreachable from any `_exit/abort/crash` path. Mac vs Linux LeakSanitizer asymmetry handled with explicit `detect_leaks=0` (no platform-divergent gate logic).
- Praise: F3 sibling NSDMI scope (10+ raw ptrs across 3 classes) correctly extends the invariant per spec REQ-3 line 49 explicit "sibling `~Model_Data` chain" — pattern-class closure, not symptom closure. Independent `delete` → `delete[]` form correction at `Lake.cpp:53-58,85-95` is a separate UB class fixed in the same commit (good cohesion).
- Praise: F6's `--dependency=afterok:$BUILD_JID` Slurm DAG with `--parsable` JID capture is canonical idiom; the `mkdir -p` before `cd` fix removes the fresh-deployment failure mode. F7 git mv preserves history.
- Praise: Phase 6 SHUD commit message documents bitwise-neutrality reasoning + explicitly identifies which fields are dead (Model_Data top-level `ISFactor`/`windH`) vs live (`hot.windH` + `Ele[i].windH`), so future maintainers won't re-add the NSDMI thinking they were "missed".
- Scope creep check: outer +SHUD touched 4 outer files + 1 git mv + cross-review/ directory + 4 SHUD class header/source files. All within REQ-3 sibling-chain carve-out per spec REQ-3 line 49 + design.md §D5 line 109. No PR-A/B/C/D work (no `tools/p8tune.F/`, no Hypre, no ADR-0007, no master plan touch).
- Tasks §1.13/§1.14/§1.15 orchestrator-owned (PR open / merge / close issue) — correctly excluded.
- Linus `delete[] nullptr` reminder: per [expr.delete]/2, deleting nullptr is a defined no-op. NSDMI brace-init `ts[MAXQUE+1] = {nullptr}` is the canonical idiom for raw-ptr arrays. Phase 6 fix is C++03/11/14/17/20 safe.

Verdict: APPROVE
