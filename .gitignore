.DS_Store

# my-agents installed projections (local only)
.claude/

# rSHUD source clone (reference only, not part of this repo)
tools/rSHUD/

# Agent guide contains server endpoints + paths — keep local-only
CLAUDE.md

# OpenSpec transient changes (glossary.md & specs stay tracked)
openspec/changes/

# subagent-workflow round-by-round working notes (issue brief / PR body / verifier verdict / review notes)
.workplans/
