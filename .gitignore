.DS_Store

# my-agents installed projections (local only)
.claude/

# rSHUD source clone (reference only, not part of this repo)
tools/rSHUD/

# Agent guide contains server endpoints + paths — keep local-only
CLAUDE.md

# OpenSpec transient changes (glossary.md & specs stay tracked)
openspec/changes/

# Stage scratch run directories (.sX-runs/ + .sNNN-runs/ + numeric variants)
.s*-runs/
.s2-103/
.s6c12a-runs/

# P1d epic scratch run directories (server Slurm + Mac local run scratch)
.p1d-runs/
.p1d-pr-*-runs/

# Per-PR review snapshots (local notes, not part of evidence chain)
docs/review/

# Python cache
**/__pycache__/
